name: billwise
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # أدوات عامة
  intl: ^0.19.0
  uuid: ^4.4.2
  image_picker: ^1.0.7
  google_mlkit_text_recognition: ^0.12.0

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  firebase_database: ^10.5.7

  # Cupertino Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # حزمة قواعد lint المقترحة
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
